{
  "name": "vertigo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "esbuild docs/src/* --bundle --servedir=docs --outdir=docs/vertigo/build",
    "build": "esbuild docs/src/* --bundle --outdir=docs/build --minify --sourcemap"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.14.47",
    "file-saver": "^2.0.5",
    "node-sass": "^7.0.1",
    "typescript": "^4.7.4"
  }
}
